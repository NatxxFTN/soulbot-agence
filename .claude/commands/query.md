# /query — Alias court de /wiki-query

Cette commande est un alias court de /wiki-query.

Voir la commande complète dans .claude/commands/wiki-query.md

Exécute EXACTEMENT le même workflow que /wiki-query.
